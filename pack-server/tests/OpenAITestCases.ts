export enum OpenAITestCases {
  EXPECT_LOG_FILE_EXISTS = 'Log file should exist',
  EXPECT_0_ERRORS_IN_RESPONSE = 'No error messages in response',
  EXPECT_GREATDAY_IN_OUTPUT_TRANSCRIPT = 'Output transcript contains "great" and "day"',
  EXPECT_GREATDAY_IN_INPUT_TRANSCRIPT = 'Input transcript contains "great" and "day"',
  EXPECT_CONNECTION_SUCCESS = 'Connection successful'
}
